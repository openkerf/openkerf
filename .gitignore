# Upstream MeerK40t-clone — geen submodule, bewust buiten versiebeheer.
# Herstellen met: git clone --depth 100 https://github.com/meerk40t/meerk40t.git meerk40t
meerk40t/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Node / frontend
node_modules/
.svelte-kit/
frontend/build/

# Editors & OS
.DS_Store
.idea/
.vscode/
*.swp

# The working record, screenshots and process notes live in a private repository
# of their own; keep them out of the public history.
workshop/

# Belt and braces for the same rule: a test or a script that writes its evidence into
# the tree writes it into somebody's commit. `curves.test.ts` did, from `../../` — twice
# five PNGs were swept in by a `git add -A` before anybody noticed.
/screenshots/
